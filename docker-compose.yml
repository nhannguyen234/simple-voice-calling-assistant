version: '3'
services:
  agent-app:
    container_name: synergies_ai_agent
    image: synergies_ai
    build:
      context: .
    ports:
      - "8000:80"
    env_file:
      - ".env"
    volumes:
      - .:/code
    working_dir: /code
    command: python /code/main.py